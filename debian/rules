#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

clean:
	dh clean --with autotools-dev
	autoreconf -f -i
	./configure

%:
	dh $@  --with autotools-dev
